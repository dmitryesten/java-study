'use strict';

function print(array = [], separator){
    let elementsArray;
    for (var index = 0; index < array.length; index++) {
        elementsArray += array[i] + separator;
    
    return elementsArray;
};